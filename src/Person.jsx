
import { use } from "react"

export default function Person({personas}){

   const firnd = use(personas);
    return (
        <div className="broders">
            <h2>Persones :fir</h2>
        </div>
    )
}